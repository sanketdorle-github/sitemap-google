import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default {
  base: '/',
};

// export default defineConfig({
//   plugins: [react()],build: {
//     outDir: 'dist', // Ensure the build files are outputted to the 'dist' directory
//   },
// })
